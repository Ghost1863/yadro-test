- name: Refresh apt cache
  ansible.builtin.apt:
    update_cache: true

- name: Install prerequisite packages
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
    state: present

- name: Ensure apt keyrings directory exists
  ansible.builtin.file:
    path: "{{ docker_apt_keyring_path | dirname }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Install Docker GPG key
  ansible.builtin.get_url:
    url: "{{ docker_apt_gpg_key_url }}"
    dest: "{{ docker_apt_keyring_path }}"
    owner: root
    group: root
    mode: "0644"
    force: false

- name: Add Docker apt repository
  ansible.builtin.apt_repository:
    repo: >-
      deb [arch={{ docker_apt_arch }} signed-by={{ docker_apt_keyring_path }}]
      {{ docker_apt_repository_url }} {{ ansible_facts['distribution_release'] }}
      {{ docker_apt_release_channel }}
    state: present
    filename: docker
    update_cache: true

- name: Install Docker Engine and plugins
  ansible.builtin.apt:
    name: "{{ docker_packages }}"
    state: present

- name: Install Python Docker SDK for Ansible community.docker modules
  ansible.builtin.apt:
    name:
      - python3-docker
      - python3-requests
    state: present

- name: Ensure docker service is started and enabled
  ansible.builtin.systemd_service:
    name: docker
    state: "{{ docker_service_state }}"
    enabled: "{{ docker_service_enabled }}"

- name: Ensure docker group exists
  ansible.builtin.group:
    name: docker
    state: present

- name: Add users to docker group
  ansible.builtin.user:
    name: "{{ item }}"
    groups: docker
    append: true
  loop: "{{ docker_users }}"
  when: docker_users | length > 0

- name: Verify docker installation
  ansible.builtin.command:
    cmd: docker --version
  register: docker_version_result
  changed_when: false

- name: Show docker version
  ansible.builtin.debug:
    msg: "{{ docker_version_result.stdout }}"
